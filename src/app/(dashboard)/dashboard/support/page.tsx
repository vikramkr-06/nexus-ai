import { SupportContent } from "./support-content";

export const metadata = {
  title: "Support — NexusAI",
  description: "Get help and support",
};

export default function SupportPage() {
  return <SupportContent />;
}
