import { HistoryContent } from "./history-content";

export const metadata = {
  title: "History — NexusAI",
  description: "View your AI tool usage history",
};

export default function HistoryPage() {
  return <HistoryContent />;
}
