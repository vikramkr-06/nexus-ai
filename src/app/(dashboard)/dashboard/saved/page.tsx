import { SavedContent } from "./saved-content";

export const metadata = {
  title: "Saved Results — NexusAI",
  description: "View your saved AI results",
};

export default function SavedPage() {
  return <SavedContent />;
}
